import { Routes } from '@angular/router';
import { DepenseComponent } from 'app/spiral-erp/depense/depense.component';

export const depenseRoutes: Routes = [{ path: 'depenses', component: DepenseComponent }];
